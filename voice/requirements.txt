# MATE Voice — dependencias Python
# Requiere Python 3.10 – 3.12 (Windows 64-bit)
#
# Setup inicial:
#   python -m venv mate-wakeword-env
#   .\mate-wakeword-env\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# O usar setup_mate.ps1 que hace todo automáticamente.

# ─── UI ───────────────────────────────────────────────────────────────────────
PyQt6>=6.6,<6.8

# ─── Wake word ────────────────────────────────────────────────────────────────
openwakeword>=0.6.0
onnxruntime>=1.17.0

# ─── STT (Speech-to-Text) ────────────────────────────────────────────────────
# El modelo "medium" se descarga automáticamente en el primer arranque (~1.5 GB)
faster-whisper>=1.0.0

# ─── TTS (Text-to-Speech) ────────────────────────────────────────────────────
pyttsx3>=2.90

# ─── Audio I/O ────────────────────────────────────────────────────────────────
sounddevice>=0.4.6
numpy>=1.24.0

# ─── Control del sistema ─────────────────────────────────────────────────────
pyautogui>=0.9.54
pygetwindow>=0.0.9
screen-brightness-control>=0.22.0
pycaw>=20240210              # control de volumen Windows (COM/WASAPI)
pywin32>=306                 # winreg, COM, etc.

# ─── Imágenes (visión de pantalla) ───────────────────────────────────────────
Pillow>=10.0.0

# ─── HTTP ─────────────────────────────────────────────────────────────────────
requests>=2.31.0

# ─── Web tools (F9) ───────────────────────────────────────────────────────────
feedparser>=6.0.10           # noticias RSS
duckduckgo-search>=6.0.0     # búsqueda web sin API key

# ─── Spotify (F10) ────────────────────────────────────────────────────────────
spotipy>=2.23.0

# ─── Monitor proactivo (07_monitor.py) ───────────────────────────────────────
edge-tts>=6.1.9
pydub>=0.25.1
win10toast>=0.9

# ─── Speaker recognition (03_service.py) ─────────────────────────────────────
resemblyzer>=0.1.4
